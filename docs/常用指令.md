# 1. 构建 LLVM Pass + 运行时库
cd /src/my_pass/build
cmake ..
make -j4

# 2. 运行完整插桩测试流水线
make run_tests

# 3. 编译 AFL++ 插桩版本
make compile_afl_test

# 4. 构建自定义变异器
make build_mutator

# 5. 运行模糊测试实验
AFL_NO_UI=1 afl-fuzz -i experiment_seeds -o experiment_output \
    -V 60 -- ./test_afl_instr @@

--- 

# afl的基本使用
编译 ： afl-clang-fast -o test test.c
测试：  afl-fuzz -i in -o output ./test @@

# docker
docker run -it --rm -v "$(pwd):/src" aflplusplus/aflplusplus

# 获取数组基本信息
build 目录下：cmake .. 然后 make 然后 make run_tests